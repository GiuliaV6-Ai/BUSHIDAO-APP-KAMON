const data=window.KAMON_DATA;const state={items:data.items,query:'',kana:'',category:'',categoryQuery:'',sort:'name',current:null};const $=s=>document.querySelector(s);const els={total:$('#totalCount'),result:$('#resultCount'),search:$('#searchInput'),categorySearch:$('#categorySearchInput'),kana:$('#kanaSelect'),category:$('#categorySelect'),sort:$('#sortSelect'),grid:$('#grid'),dialog:$('#dialog'),close:$('#closeDialog'),download:$('#downloadButton'),detailName:$('#detailName'),detailMeta:$('#detailMeta'),detailSource:$('#detailSource'),modalCrest:document.querySelector('.modal-crest')};els.total.textContent=data.count.toLocaleString();const collator=new Intl.Collator('ja');const spriteImages=new Map();for(const sprite of data.sprites){const image=new Image();image.src=sprite;spriteImages.set(sprite,image)}function norm(v){return v.normalize('NFKC').toLowerCase().trim()}function crestStyle(item){return`background-image:url("${item.sheet}");background-position:-${item.x}px -${item.y}px`}function fillSelect(select,values,selectedValue=''){select.replaceChildren(new Option('すべて',''));for(const value of values){select.add(new Option(value,value))}select.value=values.includes(selectedValue)?selectedValue:''}function renderCategoryOptions(){const q=norm(state.categoryQuery);const categories=data.categories.filter(category=>!q||norm(category).includes(q));fillSelect(els.category,categories,state.category);if(state.category&&!categories.includes(state.category)){state.category=''}}function filter(){const q=norm(state.query);const categoryQuery=norm(state.categoryQuery);let list=state.items.filter(i=>(!state.kana||i.kana===state.kana)&&(!state.category||i.category===state.category)&&(!categoryQuery||norm(i.category).includes(categoryQuery))&&(!q||norm(i.search).includes(q)));list.sort((a,b)=>state.sort==='category'?collator.compare(a.category,b.category)||collator.compare(a.name,b.name):collator.compare(a.name,b.name));render(list)}function render(list){els.result.textContent=`${list.length.toLocaleString()}件`;els.grid.replaceChildren();const frag=document.createDocumentFragment();for(const item of list.slice(0,300)){const card=document.createElement('button');card.type='button';card.className='card';card.innerHTML=`<div class="crest" style="${crestStyle(item)}"></div><h3>${item.name}</h3><p>${item.kana} / ${item.category}</p>`;card.addEventListener('click',()=>openDetail(item));frag.append(card)}els.grid.append(frag)}function openDetail(item){state.current=item;els.modalCrest.setAttribute('style',crestStyle(item));els.detailName.textContent=item.name;els.detailMeta.textContent=`${item.kana} / ${item.category}`;els.detailSource.textContent=item.source;els.dialog.showModal()}function safeFileName(value){return value.normalize('NFKC').replace(/[\\/:*?"<>|#%&{}$!`@+=]/g,'-').replace(/\s+/g,'-').replace(/-+/g,'-').replace(/^-|-$/g,'')||'kamon'}function downloadCurrent(){const item=state.current;if(!item)return;const spriteImage=spriteImages.get(item.sheet);const run=()=>{const canvas=document.createElement('canvas');canvas.width=data.cell;canvas.height=data.cell;const ctx=canvas.getContext('2d');ctx.drawImage(spriteImage,item.x,item.y,data.cell,data.cell,0,0,data.cell,data.cell);canvas.toBlob(blob=>{const a=document.createElement('a');const url=URL.createObjectURL(blob);a.href=url;a.download=`${safeFileName(item.name)}.png`;a.click();URL.revokeObjectURL(url)},'image/png')};if(spriteImage.complete)run();else spriteImage.addEventListener('load',run,{once:true})}fillSelect(els.kana,data.kana);renderCategoryOptions();els.search.addEventListener('input',e=>{state.query=e.target.value;filter()});els.categorySearch.addEventListener('input',e=>{state.categoryQuery=e.target.value;renderCategoryOptions();filter()});els.kana.addEventListener('change',e=>{state.kana=e.target.value;filter()});els.category.addEventListener('change',e=>{state.category=e.target.value;filter()});els.sort.addEventListener('change',e=>{state.sort=e.target.value;filter()});els.close.addEventListener('click',()=>els.dialog.close());els.download.addEventListener('click',downloadCurrent);filter();